# Vertical capture instructions (9:16)

1. Open the current RustChain `docs/API_REFERENCE.md` in a browser at the `GET /epoch` section. Crop vertically around the endpoint heading and example JSON.
2. Highlight `blocks_per_epoch: 144` and the description that 144 slots is approximately 24 hours.
3. Pan to the example fields `epoch`, `slot`, `epoch_pot`, `enrolled_miners`, and `total_supply_rtc` while the narration names them.
4. Add a full-screen text card for 2–3 seconds: **NETWORK-LEVEL DATA ≠ GUARANTEED INDIVIDUAL PAYOUT**.
5. End on the canonical repository link: `https://github.com/Scottcjn/Rustchain`.

No third-party media is needed. Use only screen capture of the public repository/API reference plus plain text overlays.